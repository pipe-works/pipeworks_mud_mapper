[paths]
# Relative paths resolve from the project root.
# Absolute paths are allowed.
db_path = data/mapper.db
zones_dir = data/zones
# Optional override for the MUD server world index.
world_json_path = data/world.json
